apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: prometheus-tls
  namespace: observability

spec:

  secretName: prometheus-tls

  issuerRef:
    name: local-ca
    kind: ClusterIssuer

  dnsNames:
    - prometheus.local