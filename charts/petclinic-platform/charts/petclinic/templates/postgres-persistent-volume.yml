apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-backups
spec:
  storageClassName: ""
  capacity:
    storage: 5Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /host-backups