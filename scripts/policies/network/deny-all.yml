apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
  namespace: petclinic
  spec:
    podSelector: {} # All pods in the namespace
    policyTypes:
    - Ingress
    - Egress